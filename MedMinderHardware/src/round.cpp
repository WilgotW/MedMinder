int roundnum();

int roundangle(float angle, float rest) {
    float wangle = angle + rest;
    int wholeangle = wangle;
    return wholeangle;
}
float roundrest(float angle, float rest) {
    float a = angle + rest;
    int b = a
    float c = b;
    float res = c-a;
    return res;
}